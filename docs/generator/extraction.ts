import * as TypedocJSON from '../typedocs/web/test_components/Badge.json'

type DocType = typeof TypedocJSON

function extractionIntersection(part: DocType['children'][0]['type']) {
  
  // if () {

  // }
}
